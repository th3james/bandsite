<h1>Listing images</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @images.each do |image| %>
  <tr>
    <td><%= link_to image_tag(image.image.url), image %></td>
    <td><%= link_to 'Show', image %></td>
  </tr>
<% end %>
</table>

<br />
