<div class="news_post">
  <div class="post_title">
    <h2>Image Channel</h2>
  </div>
  <div class="post_body">
    <ul class="photo">
      <% @photos.each do |photo| %>
        <li><%= link_to image_tag(photo.image_url(:thumb).to_s), photo %></a></li>
      <% end %>
    </ul>
  </div>

  <br />

  <%= link_to 'New photo', new_photo_path %>
</div>

